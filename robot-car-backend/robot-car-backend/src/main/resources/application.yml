spring:
  datasource:
    url: jdbc:mysql://robotcar-db-server.mysql.database.azure.com:3306/robotcardb?useSSL=true&requireSSL=false
    username: adminrobot
    password: Lphuc1215@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true